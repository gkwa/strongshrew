[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "strongshrew"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "pyperclip",
]

[project.scripts]
strongshrew = "strongshrew.cli:main"

[tool.ruff]
line-length = 88
